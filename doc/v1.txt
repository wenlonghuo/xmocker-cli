改进要点：
  1. 所有配置在前端全部列出，图形配合JSON串
  // 2. 拆分页面至独立文件夹
  // 3. 数据库目录存放在 ~ 文件夹下
  // 4. 原数据库导入功能
  5. 数据库结构调整，增加各接口间关联功能，包括新增或修改或返回对应值，原各种配置信息修改为config子目录
  6. schema共用，减少输入次数
  // 7. 编辑器使用codemirror
  8. mock数据存储添加新表，存储提交类的接口
  9. schema使用标准类型
  10. 自带分页功能
  11. 智能识别Mock对应的schema
  // 12. 与fiddler配合实现代理，404都转至指定域名
  13. 错误日志收集
  14. 404转至公用的库或所有的API列表，即extend功能
  15. 数据上传至服务器
  16. 用户系统
  17. api自动生成文档与读取远程文档或本地文档
  // 18. 去除随机功能
  19. 抛异常代码404
  20. 自定义快捷消息模板，如code: 401等
  21. 利用代理自动抓取真实server数据，存放于Mock中，并进行相应的提示修改
  22. 数据处理后调用返回函数
  23. 全局公用api
  // 24. 快捷捷启动停止退出等
  // 25. 延时设置
  -- 26. 为项目添加统一前缀 api
  27. 添加eruda方便手机调试，或weinre

api返回数据修改模式
  新建数据表，根据指定api的定位，如 $api['get.getactlist.func'],添加 add 即新数据（参数中传入faker）， modify修改指定值，delete删除指定值。每次取数据时从表中获取
  api指定关联表

schema共用模式
  新建schema模板表，模板更新后可选择是否更新到子模板。原表中应该存储模板表中ID
  错误模板共用功能
  新建表


分页
指定输入的no,size, 输出对应的no, size,定义总页数，复用mock数据中提供的内容，或者使用模板生成数据，暂时使用Mock,使用Mock时添加序号标识

数据同步新机制
  api仍旧使用uuid
  使同一方法下，同一识别路径，同一名称作为api唯一性判断
  添加识别路径上匹配的字符串，不再使用名称作为判断，可使用中文
  添加api同步时合并功能，项目同步时合并功能
  粒度细分至独立配置项

项目同步使用uid, 可单个配置项独立下载




